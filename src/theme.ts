// src/theme.ts
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Your-Font-Family, sans-serif", // 원하는 글꼴로 변경
  },
});

export default theme;
